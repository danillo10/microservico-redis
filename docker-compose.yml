version: "3"

services:
  redis:
    image: redis
    container_name: redis
    ports:
      - "6379:6379"
    restart: always
    command: redis-server --appendonly yes --replica-read-only no
    networks:
      - funeraria-network

networks:
  funeraria-network:
    name: funeraria-network

